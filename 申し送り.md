# 申し送り — iPhone と Mac の受け渡し帳

iPhone のクロードと Mac のクロードは、**記憶を共有しません**。
セッションはそれぞれ独立していて、片方が昨日話したことを、もう片方は何ひとつ知りません。
共有できるのは、このリポジトリだけです。**この一枚が、二台をつなぐ唯一の場所**です。


⚠️ **このリポジトリは公開（public）で、GitHub Pages が配信しています。**
この帳面も `https://gcad.engawaya.com/申し送り.md` として誰でも読めます。
**まだ言っていない予定・値段・取引先の名前は、ここへ書かないこと。**
公にできない相談は、非公開リポジトリ（`GCad` / `Ochigee_iOS_v2`）側の申し送りへ。

## 役割

| どこ | すること | しないこと |
|---|---|---|
| **iPhone** | 相談・思いつき・気づいたこと。ここへ書いて push するところまで | **コードは書かない**。画面が小さく、動かして確かめる手立てが無いので、書けば必ず確認のない変更になる |
| **Mac** | 設計と実装、実機での確認 | **未処理を読まずに始めない** |

## 使い方

- iPhone で **`/idea 思いついたこと`** … 「未処理」の先頭に足して、commit と push までやります
- Mac で **`/todo`** … 「未処理」を読み上げ、着手するものを決め、終わったら「済」へ送ります

手で書き足しても構いません。書式より、**未処理に残っていること**のほうが大事です。

⚠️ **済んだものを消さないこと。** 見送ったものも理由ごと残します。
消すと、同じ思いつきが半年後にまた出てきて、また同じ検討をすることになります。

---

## 未処理

### 2026-07-27　この二枚が main に載っていない
（webから）
受け渡し帳を置いた日の web セッションは、**この帳面も CLAUDE.md も読まずに始まりました。**
二枚が `claude/android-to-ios-port-5jal54` にだけ載っていて、`main` に無かったためです。

前提：web のセッションは毎回リポジトリを取り直し、`main` から枝を切って始まります。
CLAUDE.md がセッションの頭で自動的に読まれるのは本当ですが、それは
**そのとき取り出しているブランチに載っている場合だけ**です。置き場所が枝の上にあると、
枝の外から来たクロードには最初の一枚が見えません。防ごうとした事故が、一度起きました。

確かめること：`main` へ載せるだけで直ります。既存ファイルには触れない純粋な追加です
（`CLAUDE.md` / `申し送り.md` / `.claude/commands/idea.md` / `.claude/commands/todo.md`）。
`claude/biei-philosophy-tree-qk0ia1`（この枝）にも同じ二枚を積んであるので、
どちらの枝から main へ入れても構いません。

⚠️ main へ出すと GitHub Pages が配ります。`gcad.engawaya.com/CLAUDE.md` と
`.../申し送り.md` が誰でも読める状態になります（憲法に織り込み済みですが、押す前にもう一度）。
施主の許可がまだなので、web の側では main を触っていません。

---

## 済

### 2026-07-27　受け渡し帳を置いた　→ 済
（Macから）
やったこと：この `申し送り.md` と、`.claude/commands/idea.md` `.claude/commands/todo.md` を置いた。
CLAUDE.md に「iPhone と Mac の受け渡し」の節を足して、セッションの頭で必ずここへ来るようにした。
なぜそうしたか：三台のクロード（iPhone・Mac・web）は記憶を共有しない。共有できるのは
リポジトリだけで、`CLAUDE.md` はセッション開始時に自動で読まれる。そこから一枚だけ
指させば、受け渡しは成立する。
外した案：①専用のサービスや外部のメモ帳を立てる案 — 縁側屋の作法「外から何も読み込まない」に
反するうえ、止まったら受け渡しごと消える。②Issue で受け渡す案 — 非公開リポジトリでも
使えるが、`CLAUDE.md` から自動では読まれないので「読まずに始める」事故が残る。
リポジトリの中の一枚が、いちばん壊れにくい。
