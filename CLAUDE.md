# CLAUDE.md — K'Ad蔵人 Web版（配信用）の憲法

このファイルは作業のたびに読み込まれる「プロジェクトの決まり事」です。
施主＝岡澤博喜さん（「博喜さん」と呼ぶ）。Claudeの呼び名は「蔵人」（安祖艪蔵人）。

⚠️**このリポジトリは公開（public）で、GitHub Pages が配信しています。**
このファイルも `https://gcad.engawaya.com/CLAUDE.md` として誰でも読めます。
**書けないことは書かない**——設計の込み入った話は非公開の `GCad` 側へ。

## iPhone と Mac の受け渡し（役割分担）

⚠️ **セッションを始めたら、まず `申し送り.md` の「未処理」を読むこと。**

iPhone のクロード・Mac のクロード・web のクロードは、**記憶を共有しません**。
片方が昨日話したことを、もう片方は何ひとつ知りません。
共有できるのはこのリポジトリだけで、`申し送り.md` がその受け渡し口です。

| どこ | すること | しないこと |
|---|---|---|
| **iPhone** | 相談・思いつき・気づいたこと。`申し送り.md` へ書いて push するところまで | **コードは書かない**。動かして確かめる手立てが無いので、書けば必ず確認のない変更になる |
| **Mac** | 設計と実装、実機での確認 | **未処理を読まずに始めない** |

- iPhone で **`/idea 思いついたこと`** … 「未処理」の先頭に足して、commit と push まで
- Mac で **`/todo`** … 「未処理」を読み上げ、着手し、終わったら結果ごと「済」へ送る

済んだものも見送ったものも**消しません**。理由ごと残します。
消すと、同じ思いつきが半年後にまた出てきて、また同じ検討をすることになります。

## ここは何か — **ソースではなく、配る棚**

`https://gcad.engawaya.com/`（旧 `highlylights0-hub.github.io/gcad-web` も生きている）で
配っている **K'Ad蔵人（無料の実務2D CAD）の Web版の、ビルド成果物だけ**を置いた棚です。

| ファイル | 中身 |
|---|---|
| `index.html` | 玄関。免責と連絡先が入っている |
| `index.js` / `index.wasm` / `index.data` | Emscripten の出力（C++ / Siv3D を WebAssembly へ） |
| `CNAME` | `gcad.engawaya.com` |
| `.nojekyll` | GitHub Pages に Jekyll を通させないための空ファイル。**消さないこと** |

### ⚠️ ここを直接編集しないこと
`index.js` / `index.wasm` / `index.data` は**手で書き換えるものではありません**。
本体は非公開リポジトリ **`GCad`**（C++ / Siv3D 0.6.16）。
更新は `GCad/Web/配る.sh` を回した出力をここへ置き、push します。

**憲法・設計思想・進行中の宿題は、すべて `GCad/CLAUDE.md`** にあります。
中身の話をするときは、必ずそちらを先に読んでください。ここには配り方だけ書いてあります。

`index.html` の文言（免責・連絡先）だけは、ここで直すこともあります。
その場合も `GCad/Web/` 側の元と食い違わせないこと。**同じ文面が二か所にあると必ずずれます。**

## ⚠️ 帯域の天井（宣伝の前に必ず読むこと）
**初回訪問1人あたり実測 38.9MB。** うち `index.data` が 37.5MB（Siv3D同梱の書体。圧縮が効かない）。
GitHub Pages の 100GB/月では **月 2,632 人で頭打ち**。

**宣伝を始める前に、削る工事が要ります。** ここは「あとで考える」では間に合わない種類の
天井です（上限に当たると配信が止まる＝道具そのものが消える）。

## 触り方の作法
- **危険な操作（push＝公開 / 削除）は必ず確認**。`git commit`（ローカル）は可。
  ⚠️ここは**pushした瞬間に世界へ出る**（GitHub Pages）。
- push は GitHub Desktop の「Push origin」でも可（施主の手順）。
- 出したら、実際にブラウザで `https://gcad.engawaya.com/` を開いて確かめる。
  キャッシュが残るので、確認は**強制再読み込み**で。

## 応答と文体
- 応答・コメントは必ず**日本語**で。「何を」だけでなく**「なぜ」**まで書く。
- コミットメッセージは「**焼き直し：〇期〇号：見出し**」の流儀（`GCad` と通し番号を共有）。
  判断の理由と、外した案とその理由まで残す。

## 縁側屋のほかの棟
母屋（サイト全体）の作法は `engawaya/CLAUDE.md`、本体の憲法は `GCad/CLAUDE.md`、
Ochi-Gee（iPhone用の落ちものパズル）は `Ochigee_iOS_v2/CLAUDE.md` にあります。
